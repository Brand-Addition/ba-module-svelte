# Block third-party dependencies from running compilation/install scripts
ignore-scripts=true

# Prevent npm from pulling dependencies over insecure HTTP channels
strict-ssl=true

# Opt-out of anonymous telemetry tracking
audit=true
